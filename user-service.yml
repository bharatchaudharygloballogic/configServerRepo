eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  service-url:
    defaultZone: http://localhost8761/eureka
 spring:
  data:
   mongodb:
    host: localhost
    port: 27017
    database: players
  datasource:
   driver-name: mongodb.jdbc.MongoDriver
 server:
  port: 8081
